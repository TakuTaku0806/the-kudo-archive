<!DOCTYPE html>
<html>
  <head>
    <title>The Kudo Archive</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-gray-50 font-sans">
    <!-- TYLKO JEDNA nawigacja, TYLKO JEDNO miejsce z linkami użytkownika -->
    <nav class="bg-white shadow-sm">
      <div class="container mx-auto px-4 py-4 flex justify-between items-center">
        <a href="/" class="text-2xl font-bold text-indigo-600">The Kudo Archive</a>

        <div class="hidden md:flex space-x-6">
          <a href="/" class="text-gray-700 hover:text-indigo-600">Home</a>
          <%= link_to "Reviews", posts_path, class: "text-gray-700 hover:text-indigo-600" %>
          <%= link_to "Anime", anime_index_path, class: "text-gray-700 hover:text-indigo-600" %>
          <%= link_to "Manga", manga_path, class: "text-gray-700 hover:text-indigo-600" %>
          <% if user_signed_in? && current_user.admin? %>
            <%= link_to "Dashboard", admin_dashboard_path, class: "text-gray-700 hover:text-indigo-600" %>
          <% end %>
          <%= link_to "About", about_path, class: "text-gray-700 hover:text-indigo-600" %>
        </div>

        <div class="flex items-center space-x-4">
          <input type="search" placeholder="search..." class="border rounded-full py-1 px-4 text-sm hidden md:block">

          <!-- ✅ TYLKO TUTAJ są linki użytkownika -->
          <div class="flex items-center space-x-3 text-sm">
            <% if user_signed_in? %>
              <span class="text-gray-700">Welcome, <span class="font-semibold text-indigo-600"><%= current_user.username %></span></span>
              <%= link_to "Logout", destroy_user_session_path,
                          data: { turbo_method: :delete },
                          class: "bg-indigo-600 text-white px-3 py-1.5 rounded-lg hover:bg-indigo-700 transition" %>
            <% else %>
              <%= link_to "Sign In", new_user_session_path,
                          class: "text-indigo-600 hover:text-indigo-800 font-medium" %>
              <%= link_to "Sign Up", new_user_registration_path,
                          class: "bg-indigo-600 text-white px-3 py-1.5 rounded-lg hover:bg-indigo-700 transition" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <!-- Główna treść -->
    <main>
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-900 text-white mt-12 py-8">
      <div class="container mx-auto px-4 text-center text-sm">
        © 2026 The Kudo Archive – All about anime & manga
      </div>
    </footer>
  </body>
</html>